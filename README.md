# Wanderlust

AI-powered travel planner built with Next.js. Generate trip plans, explore recent itineraries, and discover destinations with rich imagery.


GEMINI_API_KEY=
UNSPLASH_API_KEY=
MONGODB_URI=
BASE_URL=http://localhost:3000/api


## Notes
- `BASE_URL` should match your local or deployed URL (append `/api`).
- Keep API keys private and out of version control.